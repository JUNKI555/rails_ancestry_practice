<%= form_with(model: issue_closure) do |form| %>
  <% if issue_closure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue_closure.errors.count, "error") %> prohibited this issue_closure from being saved:</h2>

      <ul>
        <% issue_closure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :epic_closure_id %>
    <%= form.text_field :epic_closure_id %>
  </div>

  <div class="field">
    <%= form.label :issue_name %>
    <%= form.text_field :issue_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
