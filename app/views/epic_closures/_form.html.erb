<%= form_with(model: epic_closure) do |form| %>
  <% if epic_closure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(epic_closure.errors.count, "error") %> prohibited this epic_closure from being saved:</h2>

      <ul>
        <% epic_closure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :board_closure_id %>
    <%= form.text_field :board_closure_id %>
  </div>

  <div class="field">
    <%= form.label :epic_name %>
    <%= form.text_field :epic_name %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
